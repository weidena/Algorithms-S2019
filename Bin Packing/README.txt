Last to Start
By Aurora Weiden
Due Date: August 17th, 2019

For binpack.py the user only needs to run "python binpack.py" in the command line and have an bin.txt file of the correct
format in their current directory

binpack.py:
- readInFile function takes in a file called bins.txt and creates an array of the items for each case and the Capacity for each case
- Each of the three algorithms is run
- the Bins and runtime of each is printed out to the consol